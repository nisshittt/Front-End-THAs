import styled from "styled-components";

const InlineFormGroup = styled.div`
  display: flex;
  flex: 1 0 auto;
  flex-flow: row wrap;
  align-items: center;
  margin-bottom: 0;
`;
InlineFormGroup.displayName = "InlineFormGroup";

export default InlineFormGroup;
